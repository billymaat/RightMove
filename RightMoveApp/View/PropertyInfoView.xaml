<UserControl x:Class="RightMove.Desktop.View.PropertyInfoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RightMove.Desktop.View"
             xmlns:main="clr-namespace:RightMove.Desktop.View.Main"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance main:MainViewModel}"
             >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <local:RightMoveImageView></local:RightMoveImageView>
        <TextBox Grid.Row="1" Text="{Binding RightMovePropertyFullSelectedItem.Desc}" TextWrapping="Wrap"></TextBox>
    </Grid>
</UserControl>
